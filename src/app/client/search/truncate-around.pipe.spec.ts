import { TruncateAroundPipe } from './truncate-around.pipe';

describe('TruncateAroundPipe', () => {
  it('create an instance', () => {
    const pipe = new TruncateAroundPipe();
    expect(pipe).toBeTruthy();
  });
});
